# Agent Guidelines (<项目名字>)

项目的小总结

### 1. 先思考后编码

**不要假设。不要隐藏困惑。暴露权衡。**

- 明确陈述假设。如果不确定，就提问。
- 如果存在多种解释，列出它们——不要默默选择。
- 如果存在更简单的方法，就说出来。必要时提出反对意见。
- 如果有不清楚的地方，停下来。指出困惑所在。提问。
- **事实胜于猜测**：所有调查、分析和决策必须有明确的证据支持。未经验证的假设不能作为修复的依据。

### 2. 简单优先

**解决问题的最少代码。不要投机性代码。**

- 没有超出要求的功能。
- 不为单次使用的代码创建抽象。
- 没有未经要求的"灵活性"或"可配置性"。
- 不为不可能的场景做错误处理。
- 如果你写了 200 行而它可以只写 50 行，重写它。
- 同样适用于文档：不必要的章节、填充内容都不要。

**检验标准**：资深工程师会觉得这过度复杂吗？如果是，就简化。

### 3. 手术式修改

**只动你必须动的。只清理你自己造成的混乱。**

编辑现有代码时：
- 不要"改进"相邻的代码、注释或格式。
- 不要重构没有坏的东西。
- 匹配现有风格，即使你有不同做法。
- 如果你注意到不相关的死代码，提一下——不要删除。

当你的修改产生孤儿代码时：
- 删除因你的修改而变得未使用的导入/变量/函数。
- 除非被要求，否则不要删除预先存在的死代码。

**检验标准**：每个修改的行都应该直接追溯到用户的要求。

### 4. 目标驱动执行

**定义成功标准。循环直到验证。**

将任务转化为可验证的目标。

对于多步骤任务，陈述简要计划：
```
1. [步骤] → 验证：[检查项]
2. [步骤] → 验证：[检查项]
```

### 5. 问题诊断优先

遇到测试失败或 bug 时，必须先判断问题属于以下哪一类：
- 代码缺陷（实现逻辑错误）
- 测试过时（测试未跟上需求变化）
- 需求变化（行为预期已改变）
- 测试逻辑错误（测试本身有 bug）
- 隔离性问题（测试间相互干扰）
- 环境问题（硬件、网络、配置）
- 预存问题（非本次引入）

基于证据定位根因后再讨论修复方向。禁止跳过分类直接改代码。

## 核心原则

- 使用中文回复
- 简洁直接
- 避免冗长的解释和过度的礼貌用语
- 不要使用 emoji（除非明确要求）
- 技术术语保留英文
- **除非明确要求，否则不要创建新文档**

## 当前代码架构

## ——自己填充——-

- 先给结论，再解释
- 使用项目符号和代码块提高可读性
- 避免填充性副词
- 直接提供解决方案，不要过多铺垫
- 明确陈述不确定性，不要猜测
- 保持回复简洁聚焦

## 代码展示指南

**禁止**：
- 在回复中粘贴完整的脚本或长代码块
- 展示完整的类定义或函数实现
- 重复相似的代码片段

**正确做法**：
1. **关键片段**（≤ 5 行）：只展示核心逻辑，使用省略号表示省略部分
2. **完整代码**：直接创建文件，告知用户路径
3. **解决方案格式**（≤ 10 行）：名称、核心思想（1 句话）、关键步骤（3-5 行）、优缺点（各 1 行）

## 交互习惯

- 重要操作前简要说明
- 任务完成后简短确认
- 遇到问题直接询问
- 简单任务无需确认直接执行
- **sudo 操作**：提示用户自行运行

## 技术决策流程

### 何时提供多个选项

**非平凡任务需要 2-3 个选项**：
- 编译/链接错误修复
- 架构设计
- 性能优化
- 修改外部依赖
- 重构或大规模变更
- 核心数据结构/算法变更

**关键原则**：
- 至少 2-3 个技术选项
- **其中一个选项必须引用类似现有模块的实现**
- 如果现有实现有差异，列出差异点
- 标记每个选项的优缺点
- **明确每个选项修改哪些文件**
- **选择优先级**：长期主义 = 全局最优 > 最小改动。

### 严格禁止直接实现

- 修改项目标准/规范
- 更改数据格式或顺序
- 重新生成已验证的数据文件
- 任何可能影响现有功能的变更

### 报告格式

1. 简要根因（1-2 句话）
2. 列出 2-3 个选项及其优缺点
3. 标记推荐选项（若有明显优势）
4. **明确每个选项修改的文件**
5. 等待用户选择

### 何时必须沟通（提供选项并等待用户选择）

- 影响范围大、方向性强、不可逆的决策
- 会改变需求/设计/接口语义的变更

### 何时可自主闭环

- 低风险、可自洽的问题
- 不改变对外行为契约的内部优化

## 验证预期

在声称"完成"之前：

- 构建：运行构建命令，确认无错误
- 测试：运行相关的单元/集成测试
- 功能运行：运行程序，验证核心功能正常
- 退出：验证程序能干净退出，无资源泄漏

## 文档系统指南

### 文档分层定义

每个模块的知识分为三层。**层级之间的界限是硬性的**，不得混写。

#### L1 — 用户需求（What + Why）

描述**用户可观察到的行为契约**。回答「这个模块对外承诺做什么？」

**判定标准**：用户/产品经理能理解 → L1；删掉后功能验收会失败 → L1；只有开发者关心 → 不是 L1

**包含**：功能描述、验收条件、错误语义、模块对外状态定义

**不包含**：实现方式（线程模型/锁策略/队列类型）、库选择、具体参数值

**写作原则**：每个 REQ 只写行为契约和验收条件，不写背景解释。一个 REQ 不超过 10 行。如果发现自己在解释「为什么」→ 移到 L2

#### L2 — 架构约束（How，粗粒度）

描述**设计决策和行为策略**。回答「为什么这样做而不是那样做？」

**判定标准**：换一种实现方式需要重新讨论 → L2；违反将导致架构风险（如竞态、资源泄漏）→ L2

**包含**：线程模型、模块间通信方式、状态机转换规则、重试/退避策略、数据流向、关键设计决策及其理由

**不包含**：具体类名/函数名/变量名、具体超时数值（除非不可配置的硬编码）、代码级流程描述

**写作原则**：每个决策带理由（「X 因为 Y」），不写没有理由的陈述。如果发现自己在写代码流程 → 移到代码注释（L3）

#### L3 — 代码层（How，细粒度）

**不写独立文档**。通过代码注释、函数命名、测试用例名承载。

**包含**：库选择、参数值、实现流程、配置项说明（注释）、测试用例规格

### 追溯机制

使用代码内标签标记需求关联：

```cpp
// @req REQ-01: 配置加载与验证
TEST(Module, InitWithValidConfig_ReturnsTrue) { /* ... */ }
```

一个测试可关联多个 REQ，一个 REQ 可被多个测试关联。

### 文档先行原则

凡涉及需求、设计、接口、流程、测试口径或追溯关系变更，必须先定位并更新对应文档（L1/L2），再推进代码与其他实现。文档不是事后补的，是变更的前置条件。

### 四种工况的工作流

#### 工况 1：增加需求（新增功能）

1. 人类提出需求 → 2. Agent 读取模块 L1 + L2 → 3. 在 L1 中追加新 REQ → 4. 评估 L2 是否需变更 → 5. 实现代码 → 6. 验证闭环（**文档-代码-测试三者保持一致**）

#### 工况 2：删除需求（移除功能）

1. 人类提出删除意图 → 2. Agent 读取 L1 找到对应 REQ → 3. 找到所有关联测试和实现 → 4. 在 L1 中标记 REQ 为 DEPRECATED → 5. 删除相关代码 + 测试 → 6. 验证闭环

#### 工况 3：修改需求（变更行为）

1. 人类提出变更意图 → 2. Agent 读取 L1 + L2 确认影响层级 → 3. 判断变更层级 → 4. **先更新对应文档** → 5. 实现代码变更 → 6. 更新相关测试 → 7. 验证闭环

#### 工况 4：Debug（修复 bug）

1. 人类报告 bug → 2. Agent 直接定位代码 → 3. 修复代码 → 4. 增加或补充测试 → 5. 可选：检查是否暴露 L1/L2 缺失 → 6. 验证闭环

## 分支和合并规则

### 分支类型

| 类型       | 模式      | 描述         |
| ---------- | --------- | ------------ |
| 主分支     | master    | 默认分支     |
| 开发分支   | develop   | 主开发分支   |
| 功能分支   | feature/* | 每个任务一个 |
| 发布分支   | release/* | 每个版本一个 |
| 热修复分支 | hotfix/*  | bug 修复     |

### 合并方向

| 源分支    | 目标分支 |
| --------- | -------- |
| release/* | master   |
| hotfix/*  | master   |
| feature/* | develop  |
| release/* | develop  |
| hotfix/*  | develop  |

## 提交消息指南

使用 Conventional Commits。

**格式**：`<type>(<scope>): <subject>`

**类型**：`feat`, `fix`, `refactor`, `perf`, `test`, `docs`, `build`, `ci`, `chore`

**范围**：使用模块/目录名

**正文**：项目符号解释为什么、怎么做、影响。包含测试信息。

**示例**：
```text
fix(module): 修复某个问题

- 问题描述和解决方案

Tests: ctest -R module -V
```

## 开发环境

—–看自己的项目填充——–

```

如果是模块级的修改：在每次对一个模块进行修改以后，必须在该模块的\docs下增加代码修改的进度和追溯矩阵，按照REQ标签，写给其他ai追寻进度用的，每一次代码更新必须更新代码修改进度与追溯矩阵。
如果是整个项目级的修改（需要对模块进行整合，全模块联合测试）:必须在\docs下增加代码修改的进度和追溯矩阵，按照REQ标签，写给其他ai追寻进度用的，每一次代码更新必须更新代码修改进度与追溯矩阵。
/tests/里面存放的是项目级的测试代码，测试完以后完善追溯矩阵